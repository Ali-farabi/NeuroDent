name: Backend

on:
  push:
    branches:
      - main
      - nextjs-setup
  pull_request:
    branches:
      - main
      - nextjs-setup

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: next-app/package-lock.json

      - name: Install Next.js dependencies
        working-directory: next-app
        run: npm ci

      - name: Check backend syntax
        run: |
          node --check next-app/backend/server.js
          node --check next-app/backend/service.js
          node --check next-app/backend/storage.js
          node --check next-app/backend/smoke-test.js

      - name: Run backend smoke test
        working-directory: next-app
        run: npm run test:backend

      - name: Run Next.js smoke test
        working-directory: next-app
        run: npm run test:e2e

      - name: Build Next.js app
        working-directory: next-app
        run: npm run build
